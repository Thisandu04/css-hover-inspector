{
  "name": "css-hover-inspector",
  "version": "1.0.0",
  "description": "A live CSS inspector overlay — Chrome extension + bookmarklet, sharing one engine.",
  "type": "module",
  "scripts": {
    "build:bookmarklet": "node bookmarklet/build.js",
    "watch:bookmarklet": "node bookmarklet/build.js --watch",
    "generate:icons": "node scripts/generate-icons.js"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "sharp": "^0.35.4"
  }
}
